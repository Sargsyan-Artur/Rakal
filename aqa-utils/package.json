{
  "name": "հհ",
  "version": "0.1.4",
  "description": "utils for aqa libs",
  "scripts": {
    "lint": "tslint -p tsconfig.json -c tslint.json --fix",
    "build": "tsc",
    "prepublishOnly": "npm run lint && npm run build",
    "prepare": "husky install"
  },
  "repository": {
    "type": "git",
    "url": "codecommit::us-east-1://rak-aqa-utils"
  },
  "files": [
    "build"
  ],
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "dependencies": {
    "log4js": "^6.3.0",
    "nconf": "^0.11.3",
    "randexp": "^0.5.3"
  },
  "devDependencies": {
    "@types/nconf": "^0.10.1",
    "@types/node": "^14.18.0",
    "husky": "^7.0.4",
    "ts-node": "^10.4.0",
    "tslint": "^6.1.3",
    "typescript": "^4.5.4"
  },
  "engines": {
    "node": ">=14.0.0 < 16.0.0",
    "npm": ">=6.14.0 < 7.0.0"
  }
}
